1. Install Prometheus
brew install prometheus
prometheus -h
prometheus --version

2. Modify YMAL Configuration File
vim /usr/local/etc/prometheus.yml

3. Run Prometheus
$ prometheus --config.file=/usr/local/etc/prometheus.yml

http://localhost:9090/
